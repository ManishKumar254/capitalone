{
  "name": "cors-proxy",
  "version": "1.0.0",
  "description": "",
  "main": "proxy.js",  // Make sure this matches your entry file
  "scripts": {
    "start": "node proxy.js",  // Add the start script
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.0",
    "node-fetch": "^3.3.2"
  }
}
